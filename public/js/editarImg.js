// Función para mostrar la vista previa de la imagen seleccionada (Imagen 5)
function mostrarVistaPrevia(input, preview) {
  const archivo = input.files[0];
  if (archivo) {
    const lector = new FileReader();
    lector.onload = function (e) {
      preview.src = e.target.result;
      preview.style.display = "block";
    };
    lector.readAsDataURL(archivo);
  }
}



// Función para mostrar la vista previa de la imagen seleccionada (Imagen 6)
function mostrarVistaPrevia(input, preview) {
  const archivo = input.files[0];
  if (archivo) {
    const lector = new FileReader();
    lector.onload = function (e) {
      preview.src = e.target.result;
      preview.style.display = "block";
    };
    lector.readAsDataURL(archivo);
  }
}



// Función para mostrar la vista previa de la imagen seleccionada (Imagen 7)
function mostrarVistaPrevia(input, preview) {
  const archivo = input.files[0];
  if (archivo) {
    const lector = new FileReader();
    lector.onload = function (e) {
      preview.src = e.target.result;
      preview.style.display = "block";
    };
    lector.readAsDataURL(archivo);
  }
}



// Función para mostrar la vista previa de la imagen seleccionada (Imagen 8)
function mostrarVistaPrevia(input, preview) {
  const archivo = input.files[0];
  if (archivo) {
    const lector = new FileReader();
    lector.onload = function (e) {
      preview.src = e.target.result;
      preview.style.display = "block";
    };
    lector.readAsDataURL(archivo);
  }
}


